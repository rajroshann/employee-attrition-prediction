# ─────────────────────────────────────────────
# Employee Attrition Prediction — Dependencies
# ─────────────────────────────────────────────
# Install with:
#   pip install -r requirements.txt
#
# Version ranges used (not exact pins) so the project
# stays portable across different OS and Python versions.
# Tested with Python 3.10, 3.11, 3.12
# ─────────────────────────────────────────────

# Web framework
flask>=2.3,<4.0

# Data & ML
pandas>=1.5
numpy>=1.24
scikit-learn>=1.3
xgboost>=1.7

# Explainability
shap>=0.42

# Model serialization
joblib>=1.2

# PDF generation (Module 8)
reportlab>=3.6

# Deployment (Gunicorn replaces Flask dev server on Render)
gunicorn>=20.1
